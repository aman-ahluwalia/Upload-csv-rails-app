<%= form_with(model: name, local: true) do |form| %>
  <% if name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(name.errors.count, "error") %> prohibited this name from being saved:</h2>

      <ul>
      <% name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :name_date %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :name_number %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :name_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
